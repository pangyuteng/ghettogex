apiVersion: v1
kind: PersistentVolume
metadata:
  namespace: hohoho
  name: nfs-pv
  labels:
    app: nfs-data
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  nfs:
    server: "1.2.3.4"
    path: "/exports" # "pvc" folder must exist in "/exports" directory of NFS server