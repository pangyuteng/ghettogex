apiVersion: v1
kind: Service
metadata:
  namespace: gg
  name: adminer
  annotations:
    metallb.universe.tf/address-pool: first-pool
spec:
  selector:
    app: adminer
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
