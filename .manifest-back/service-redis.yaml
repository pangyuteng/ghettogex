apiVersion: v1
kind: Service
metadata:
  name: fi-redis
  annotations:
    metallb.universe.tf/address-pool: first-pool
spec:
  selector:
    app: fi-redis
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
