apiVersion: v1
kind: Service
metadata:
  name: fi-redis
  annotations:
    metallb.universe.tf/address-pool: first-pool
spec:
  selector:
    app: fi-redis
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5672
    targetPort: 5672
  - protocol: TCP
    port: 15672
    targetPort: 15672
